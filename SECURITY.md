# 安全政策

请不要在公开 Issue 中披露未修复的安全漏洞、真实简历、API Key、密码、访问令牌或生产环境信息。

发现漏洞时，请通过 GitHub Security Advisory 的私密报告入口联系维护者，并提供受影响版本、复现步骤、影响范围和建议修复方式。维护者确认问题前，请避免公开利用细节。

部署方必须自行完成以下工作：

- 使用独立高强度密钥并通过部署环境注入，禁止提交 `deploy/.env`。
- 通过 HTTPS 对外提供服务，仅暴露反向代理端口。
- 按当地法律和公司制度处理简历、面试反馈及删除请求。
- 对 LLM/OCR 服务商完成数据处理、跨境传输和保留策略评估。
- 定期升级依赖、备份数据并验证恢复流程。
